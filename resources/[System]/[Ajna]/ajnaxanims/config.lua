Config = {}
Config.Controls = {}

-- ATTENTION: Key controls to stop the animation, and to increase/decrease animation speed can be changed by the player through the game menu (settings > key bindings > fivem)
-- Default keys are F6 to stop, PAGE UP to increase speed and PAGE DOWN to decrease speed

-- ATENÇÃO: Os controles para parar e aumentar/reduzir a velocidade das animações pode ser alterada pelo próprio player nas configurações no menu do jogo (configurações > teclado > fivem)
-- As teclas padrão são F6 para parar a animação, PAGE UP para aumentar a velocidade e PAGE DOWN para reduzir a velocidade


-- Language (default is 'en' for english translation)
-- Idioma (coloque 'br' para exibir o texto em português)
Config.Locale = 'br'

-- You can alter the command prefix here (default: /x)
-- Você pode alterar o comando aqui (padrão: /x)
Config.Command = 'sexo'

-- Control config for accept anim request (default: 246 - Y)
-- Configuração do controle para aceitar a animação (padrão: 246 - Y)
Config.Controls.Accept = {'Y', 246}

-- Control config for refusing anim request (default: 303 - U)
-- Configuração do controle para recusar a animação (padrão: 303 - U)
Config.Controls.Refuse = {'U', 303}