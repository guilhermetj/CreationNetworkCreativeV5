Config = {}

-- Language (default is 'en' for english translation)
-- Idioma (coloque 'br' para exibir o texto em português)
Config.Locale = 'br'

-- You can alter the command prefix here (default: /d)
-- Você pode alterar o comando aqui (padrão: /d)
Config.Command = 'dancar'

-- Set this to true if you're using Gabz's Vanilla Unicorn MLO, for the synced pole dancing
-- Altere para true se você estiver usando o mapa do Vanilla Unicorn do Gabz, para o pole dancing sincronizado
Config.UsingGabzVU = false

-- [OPTIONAL] You may set extra coords here, following the example below (with actual coords, instead of x y z)
-- [OPCIONAL] Você pode adicionar mais coordenadas aqui (com coordenadas reais, ao invés de x y z)
Config.ExtraCoords = {
	--vector3(x, y, z),
}